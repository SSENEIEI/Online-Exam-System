# OES - Online Exam System

## 📋 ภาพรวม

OES (Online Exam System) เป็นแพลตฟอร์มสอบออนไลน์ที่พัฒนาด้วยเทคโนโลยี AI ชั้นนำ ออกแบบมาเพื่อสถาบันการศึกษาและองค์กรที่ต้องการจัดการข้อสอบอย่างมีประสิทธิภาพและปลอดภัย

## ✨ คุณสมบัติหลัก

- 🤖 **AI-Powered Question Generation** - สร้างข้อสอบอัตโนมัติด้วย Google Gemini AI
- 🛡️ **Advanced Anti-Cheat System** - ระบบป้องกันการโกงแบบครอบคลุม
- ⏱️ **Smart Timer System** - ระบบจับเวลาที่ทนต่อการรีเฟรช
- 📊 **Real-time Analytics** - แดชบอร์ดวิเคราะห์ผลแบบเรียลไทม์
- 📱 **PWA Support** - รองรับการติดตั้งเป็นแอปบนมือถือ
- 🎨 **Responsive Design** - ใช้งานได้ทุกอุปกรณ์

## 🚀 การติดตั้ง

### ความต้องการระบบ

- PHP 7.4 หรือสูงกว่า
- MySQL 5.7 หรือสูงกว่า
- Web Server (Apache/Nginx)
- Google Gemini API Key

### ขั้นตอนการติดตั้ง

1. **Clone โปรเจค**
   ```bash
   git clone <repository-url>
   cd OES
   ```

2. **ตั้งค่า Environment**
   ```bash
   cp .env.example .env
   # แก้ไขค่าใน .env ให้ตรงกับระบบของคุณ
   ```

3. **สร้างฐานข้อมูล**
   ```sql
   CREATE DATABASE oes_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

4. **Import โครงสร้างฐานข้อมูล**
   ```bash
   mysql -u root -p oes_db < sql_schema.sql
   mysql -u root -p oes_db < database_updates.sql
   ```

5. **ตั้งค่า API Key**
   - เพิ่ม Google Gemini API Key ในไฟล์ `.env`
   ```
   GEMINI_API_KEY=your_api_key_here
   ```

6. **ตั้งค่า Permissions**
   ```bash
   chmod 755 logs/
   chmod 644 .env
   ```

## 📁 โครงสร้างโปรเจค

```
OES/
├── api/
│   ├── generate_exam.php
│   ├── get_exam.php
│   ├── save_exam.php
│   └── submit_exam.php
├── assets/
│   ├── css/style.css
│   └── js/{app.js,student.js}
├── config/config.php
├── database/{sql_schema.sql,database_updates.sql}
├── pages/{teacher_portal.php,student_portal.php,student_practice.php,take_exam.php,dashboard.php,exam_result.php,practice_result.php,view_submission.php}
├── logs/
├── index.php
├── .env.example
└── .env
```

## 🔧 การใช้งาน

### สำหรับครู

1. เข้าสู่ระบบผ่าน `teacher_portal.php`
2. สร้างข้อสอบด้วยการสั่ง AI
3. ได้รับรหัสข้อสอบ 6 หลัก
4. แชร์รหัสให้นักเรียน
5. ดูผลและวิเคราะห์ในแดชบอร์ด

### สำหรับนักเรียน

1. เข้าสู่ระบบผ่าน `student_portal.php`
2. กรอกรหัสข้อสอบ 6 หลัก
3. ทำข้อสอบในระบบ
4. ดูผลทันทีหลังส่งคำตอบ

## 🛡️ ระบบป้องกันการโกง

- ตรวจจับการเปลี่ยนแท็บ/หน้าต่าง
- บล็อกคีย์ลัดและคลิกขวา
- บังคับโหมดเต็มจอ
- ป้องกันการคัดลอกข้อความ
- บันทึกพฤติกรรมที่น่าสงสัยทั้งหมด

## 📊 การติดตาม

- Log files เก็บใน `logs/` directory
- Error logs อัตโนมัติ
- Activity tracking
- Anti-cheat reports

## 🔒 ความปลอดภัย

- Environment variables สำหรับข้อมูลสำคัญ
- Input validation และ sanitization
- SQL injection protection
- XSS protection
- CSRF protection (recommended)

## 🎨 การปรับแต่ง

### เปลี่ยนธีม
แก้ไขตัวแปรสีใน `style.css`:
```css
:root {
  --primary-color: #667eea;
  --secondary-color: #764ba2;
}
```

### เพิ่มภาษา
เพิ่มฟังก์ชัน localization ใน `config.php`

### ปรับแต่ง AI Prompt
แก้ไข system prompt ใน `api/generate_exam.php`

## 📱 PWA Features

- ติดตั้งเป็นแอปได้
- ทำงานออฟไลน์บางส่วน
- Push notifications (ready)
- App-like experience

## 🐛 การแก้ไขปัญหา

### ปัญหาการเชื่อมต่อฐานข้อมูล
1. ตรวจสอบค่าใน `.env`
2. ตรวจสอบ MySQL service
3. ตรวจสอบ user permissions

### ปัญหา API Key
1. ตรวจสอบ Google Gemini API Key
2. ตรวจสอบ quota และ billing
3. ตรวจสอบ network connectivity

### ปัญหา Permissions
### ปัญหาบันทึกชุดข้อสอบไม่สำเร็จ (save_exam.php)
1. เปิดไฟล์ log ล่าสุดในโฟลเดอร์ `logs/` ดูข้อความ ERROR
2. ตรวจสอบว่า payload จาก front-end มี field:
   - exam_data.exam_title
   - exam_data.difficulty
   - exam_data.questions (array)
   - แต่ละ question มี question_text, type, (options + correct_answer สำหรับ multiple_choice)
3. ตรวจสอบสิทธิ์ตารางฐานข้อมูลว่ามี: exams, questions, choices
4. ลองเปิด DevTools Network ดู response JSON (มี details แสดง error จาก MySQL)
5. ถ้าเป็น NULL timer ให้แน่ใจว่าฟิลด์ `timer_minutes` อนุญาต NULL (ตาม schema ปัจจุบัน OK)

ตัวอย่าง response error:
```json
{"error":"Database transaction failed","details":"Column 'question_number' cannot be null"}
```
แปลว่า front-end ไม่ส่ง question_number → ระบบ fallback จะใส่ลำดับให้อัตโนมัติแล้ว (เวอร์ชันล่าสุด)

```bash
chmod -R 755 OES/
chown -R www-data:www-data OES/
```

## 📞 การสนับสนุน

- เช็ค log files ใน `logs/` directory
- ดู error messages ใน browser console
- ตรวจสอบ PHP error logs

## 📄 License

Licensed under MIT License. ดู LICENSE file สำหรับรายละเอียด.

## 🏆 Credits

พัฒนาโดย OES Technology Team (ทีมก็คือทำคนเดียว T-T )
- Google Gemini AI Integration
- Modern Web Technologies
- Security Best Practices

---

**Version:** 1.0.0  
**Last Updated:** August 26, 2024

ปล. จริงๆ อยากทำเป็น java แล้ว ใช้ tidb เป็นฐานข้อมูลออนไลน์ deploy ใน vercel ก็ได้ละ แต่งานเยอะเดะค่อยมาทำนะ จุ้บๆ 
